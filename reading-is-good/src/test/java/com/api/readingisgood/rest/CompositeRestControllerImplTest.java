/*
package com.api.readingisgood.rest;

import com.api.readingisgood.models.CustomerRequest;
import com.api.readingisgood.models.CustomerResponse;
import com.api.readingisgood.service.CompositeService;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.*;
import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.BDDMockito.given;
import static org.mockito.BDDMockito.then;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
class CompositeRestControllerImplTest {

    @Mock
    private CompositeService compositeService;

    @InjectMocks
    private CompositeRestControllerImpl restController;

    @BeforeEach
    void setUp() {
        Res
    }

    @Test
    void getCustomer() {
        //given
        CustomerResponse customerResponse = new CustomerResponse();
        given(compositeService.findCustomer(any())).willReturn(customerResponse);

        //when


        //then
    }
}*/
